
function main() {

  (function () {
    'use strict';

      $(window).on('scroll' , function() {
        var nav = $('.navigation')
        var scrolled = $(window).scrollTop();
        if($(window).scrollTop() > 40) {
          $(nav).removeClass('navigation--transparent');
        }else {
          $(nav).addClass('navigation--transparent');
        }

      });


  }()); // closed unonimus function

};

main();
